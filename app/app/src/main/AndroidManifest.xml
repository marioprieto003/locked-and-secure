<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LockedAndSecure"
        tools:targetApi="31">

        <activity
            android:name="com.afundacion.lockedandsecure.launcher.Launcher"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.afundacion.lockedandsecure.login.Login"/>
        <activity android:name="com.afundacion.lockedandsecure.registro.Registro"/>
        <activity android:name="com.afundacion.lockedandsecure.grupos.CrearGrupo"/>
        <activity android:name="com.afundacion.lockedandsecure.inicio.Inicio"/>
        <activity android:name="com.afundacion.lockedandsecure.contrasenas.CrearContraseña"/>
        <activity android:name="com.afundacion.lockedandsecure.contrasenas.EditarContraseña"/>
        <activity android:name="com.afundacion.lockedandsecure.ajustes.Ajustes"/>
    </application>

</manifest>